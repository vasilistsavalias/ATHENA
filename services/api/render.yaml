services:
  - type: web
    name: thesis-archaeologist-backend
    env: python
    buildCommand: pip install -r requirements.txt && alembic upgrade head
    startCommand: python start.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
